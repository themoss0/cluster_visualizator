meta:
  environment: python
  toolchain:
    name: pip
    version: "3.14"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: app.py
  command: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
